rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1 adds the authenticated profile rules. All application data starts
    // closed so an accidental deploy cannot expose partially modelled records.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
