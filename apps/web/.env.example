# Public client config — the anon key is safe to expose, the service key is NOT.
VITE_SUPABASE_URL=https://supa.retfast.com
VITE_SUPABASE_ANON_KEY=
# Optional MapLibre style URL (MapTiler, OpenFreeMap…). Falls back to an
# OSM raster style when empty.
VITE_MAP_STYLE_URL=
