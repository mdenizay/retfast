{
  "name": "retfast",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm --prefix functions run build && npm --prefix web run build",
    "check": "npm --prefix web run check && npm --prefix mobile run check && npm --prefix functions run check",
    "dev:mobile": "npm --prefix mobile run start",
    "dev:web": "npm --prefix web run dev",
    "emulators": "firebase emulators:start --project development",
    "firebase:deploy:web:development": "VITE_FIREBASE_TARGET=development npm --prefix web run build && firebase deploy --only hosting --project development",
    "firebase:deploy:web:production": "VITE_FIREBASE_TARGET=production npm --prefix web run build && firebase deploy --only hosting --project production"
  }
}
